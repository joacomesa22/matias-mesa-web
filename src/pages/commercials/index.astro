---
import Layout from "../../layouts/Layout.astro";
import { commercials } from "./commercials";
---

<Layout title="Matías Mesa - Commercials">
  <section class="hero-commercials hero">
    <h1>Commercials</h1>
  </section>
  <section class="portfolio" id="commercials">
    {
      commercials.map((comm) => {
        return;
      })
    }
  </section>
</Layout>

<style lang="scss">
  .hero-commercials {
    background-position: top;
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: linear-gradient(
        0deg,
        rgba(0, 0, 0, 0.4),
        rgba(0, 0, 0, 0.4)
      ),
      url(./img/soldadura.jpg);
  }
</style>
